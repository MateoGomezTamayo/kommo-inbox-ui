[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Volumen para persistir .tokens.json entre reinicios
# Crear el volumen en Railway dashboard y montarlo en /data
